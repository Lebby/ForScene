/**
 * 
 */
package forscene.system.debug;

/**
 * @author blackdevil
 * 
 */
public class DebugUtil {
  /*
   * public static void printCallerStack(Object obj, String method, String msg)
   * { PlayN.log().debug("********************************");
   * PlayN.log().debug(obj + " Called " + method + " message " + msg);
   * StackTraceElement[] stackTraceElements = Thread.currentThread()
   * .getStackTrace(); for (StackTraceElement stackTraceElement :
   * stackTraceElements) { PlayN.log().debug("" + stackTraceElement); } }
   */
}
