package forscene.core.util;

import static playn.core.PlayN.keyboard;

import playn.core.Keyboard;
import playn.core.Keyboard.Event;
import playn.core.Keyboard.Listener;
import forscene.core.events.*;

public class InputFactory {
	/*public static Listener createKeyboardListener()
	{
		return new Keyboard.Adapter()
		{
			@Override
			public void onKeyDown(Event event) {
				EventMonitor.getInstance().push(new EventKeyDown(event));*/
		
				/*
				if (event.keyCode() == keyboard().KEY_DOWN)
				{
					getRoot().setOrigin(getRoot().originX(), getRoot().originY()-1);
				}
				if (event.keyCode() == keyboard().KEY_LEFT)
				{
					getRoot().setOrigin(getRoot().originX()+1, getRoot().originY());
				}
				if (event.keyCode() == keyboard().KEY_RIGHT)
				{
					getRoot().setOrigin(getRoot().originX()-1, getRoot().originY());
				}
				if (event.keyCode() == keyboard().KEY_UP)
				{
					getRoot().setOrigin(getRoot().originX(), getRoot().originY()+1);
				}
			};*/
	/*		
			@Override
			public void onKeyUp(Event event) {
				EventMonitor.getInstance().push(new EventKeyUp(event));
			};
		
);*/
	
	
}
